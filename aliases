alias ll='ls --color=auto -alhF'
alias d='dirs -v'
alias :q='exit'
for index ({1..9}) alias "$index"="cd +${index}"; unset index
alias lsof="sudo lsof -i -P -n | grep LISTEN"
alias f='selected=$(fzf) && xdg-open "$selected"'
